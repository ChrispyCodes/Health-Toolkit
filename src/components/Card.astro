---
import { Icon } from "astro-icon";
export interface Props {
  title: string;
  body: string;
  href: string;
}

const { href, title, body } = Astro.props;
---

<a
  href={href}
  class="lock max-w-sm p-6 bg-white border border-gray-200 rounded-lg shadow-md hover:bg-gray-100 dark:bg-gray-800 dark:border-gray-700 dark:hover:bg-gray-700"
>
  <div class="flex items-center mb-4">
    <div
      class="flex items-center justify-center w-12 h-12 rounded-full bg-primary-500 dark:bg-primary-700"
    >
      <Icon name="tabler:bulb" class="w-12 h-12 text-white icon-light" />
    </div>
    <h2
      class="ml-4 text-2xl font-bold tracking-tight text-gray-900 dark:text-white text-center sm:text-left"
    >
      {title}
      <span>&rarr;</span>
    </h2>
  </div>
  <p
    class="font-normal text-gray-700 dark:text-gray-400 text-md text-center sm:text-left"
  >
    {body}
  </p>
</a>

<style>
  /* .link-card {
    list-style: none;
    display: flex;
    padding: 0.15rem;
    background-image: var(--accent-gradient);
    background-size: 400%;
    border-radius: 0.5rem;
    background-position: 100%;
    transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
      0 2px 4px -2px rgba(0, 0, 0, 0.1);
  } */

  /* .link-card > a {
    width: 100%;
    text-decoration: none;
    text-align: center;
    line-height: 1.4;
    padding: 1rem 1.3rem;
    border-radius: 0.35rem;
    background-color: white;
    opacity: 0.8;
  }
  h2,
  h1 {
    margin: 0;
    font-size: 1.25rem;
    transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }
  p {
    margin-top: 0.5rem;
    margin-bottom: 0;
    color: #444;
  } */
  .link-card:is(:hover, :focus-within) {
    background-position: 0;
  }
  .link-card:is(:hover, :focus-within) h2 {
    color: rgb(var(--accent));
  }
</style>
